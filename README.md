# Resume AI OpenEnv

## Overview
This project is an AI environment where an agent improves resumes to match job descriptions.

## Features
- OpenEnv compatible (reset, step)
- LLM-powered resume optimization
- Semantic similarity scoring
- 3 difficulty tasks

## Run
pip install -r requirements.txt  
python inference.py  

## Deploy
python app.py