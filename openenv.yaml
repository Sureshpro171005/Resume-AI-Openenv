name: resume-ai-openenv
description: AI agent that optimizes resumes for job matching

actions:
  - add_skills
  - improve_experience
  - optimize_keywords
  - rewrite_summary

observation:
  - resume
  - job_description
  - current_score

reward: score improvement (0 to 1)